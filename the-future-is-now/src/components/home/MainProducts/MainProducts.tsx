export const MainProducts = () => {
    console.log(process.env.SHOPIFY_HOSTNAME)
    
    return (
        <section>
            <p>Esta es la sección central de los productos</p>
        </section>
    )
}